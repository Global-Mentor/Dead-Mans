// Глобальный объект с данными для таблицы лодаутов
window.loadoutData = {
    // Заголовки колонок (типы охотников)
    columns: ["Бомбардир", "Пиромант", "Токсик", "Вампир", "Аватар", "Всё могу х2"],
  
    // Заголовки строк (стоимость отряда)
    rows: ["100", "125", "150", "175", "200"],
  
    // Двумерный массив с объектами, содержащими путь к изображению
    // cells[row][col] соответствует ячейке [стоимость, класс]
    cells: Array.from({ length: 5 }, (_, row) =>
      Array.from({ length: 6 }, (_, col) => ({
        image: `/Loadouts/Стихии/${col + 1}-${row + 1}.png` // Путь к картинке
      }))
    )
  };
  