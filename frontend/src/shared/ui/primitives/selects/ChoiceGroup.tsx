import { RadioGroup, styled } from '@mui/material'

/** Preserves native radio arrow-key navigation and supports long translated choices. */
export const ChoiceGroup = styled(RadioGroup)({ minWidth: 0, gap: 8 })
